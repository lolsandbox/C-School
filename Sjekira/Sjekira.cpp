#include <bits/stdc++.h>

using namespace std;

int n;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    freopen("Sjekira.inp", "r", stdin);
    freopen("Sjekira.out", "w", stdout);

    cin >> 
    return 0;
}